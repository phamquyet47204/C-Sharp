<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Microsoft.Maui.Controls.Maps;assembly=Microsoft.Maui.Controls.Maps"
             x:Class="VinhKhanh.Mobile.Views.MapPage"
             Title="Phố Vĩnh Khánh"
             NavigationPage.HasNavigationBar="False">

<<<<<<< HEAD
    <Grid>

        <!-- Map full-screen -->
        <maps:Map x:Name="MainMap"
                  MapType="Street"
                  IsShowingUser="True"
                  IsZoomEnabled="True"
                  IsScrollEnabled="True"/>

        <!-- Overlay góc trên: Category + Language + Unlock -->
        <Grid ColumnDefinitions="*,Auto,Auto"
              VerticalOptions="Start"
              HorizontalOptions="Fill"
              Margin="10,48,10,0">

            <!-- Dropdown loại quán ăn -->
            <Border Grid.Column="0"
                    StrokeShape="RoundRectangle 20"
                    Stroke="Transparent"
                    BackgroundColor="White"
                    HeightRequest="40">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/>
                </Border.Shadow>
                <Picker ItemsSource="{Binding Categories}"
                        SelectedItem="{Binding SelectedCategory}"
                        Title="🍜 Loại quán"
                        FontSize="13"
                        Margin="12,0"
                        VerticalOptions="Center"/>
            </Border>

            <!-- Language Picker -->
            <Border Grid.Column="1"
                    StrokeShape="RoundRectangle 20"
                    Stroke="Transparent"
                    BackgroundColor="White"
                    HeightRequest="40"
                    WidthRequest="90"
                    Margin="6,0,0,0">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/>
                </Border.Shadow>
                <Picker ItemsSource="{Binding Languages}"
                        SelectedItem="{Binding SelectedLanguage}"
                        Title="🌐"
                        FontSize="12"
                        Margin="8,0"
                        VerticalOptions="Center"/>
            </Border>

            <!-- Unlock button -->
            <Border Grid.Column="2"
                    StrokeShape="RoundRectangle 20"
                    Stroke="Transparent"
                    BackgroundColor="#FF6B35"
                    HeightRequest="40"
                    Margin="6,0,0,0">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.2"/>
                </Border.Shadow>
                <Button Text="🔓"
                        FontSize="16"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        Padding="10,0"
                        Command="{Binding UnlockAsyncCommand}"/>
            </Border>
        </Grid>

        <!-- Overlay góc phải dưới: Zoom In / Zoom Out / MyLocation -->
        <StackLayout VerticalOptions="End"
                     HorizontalOptions="End"
                     Orientation="Vertical"
                     Spacing="8"
                     Margin="0,0,12,32">

            <Border StrokeShape="RoundRectangle 24"
                    Stroke="Transparent"
                    BackgroundColor="White"
                    WidthRequest="48"
                    HeightRequest="48">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/>
                </Border.Shadow>
                <Button Text="+"
                        FontSize="22"
                        FontAttributes="Bold"
                        BackgroundColor="Transparent"
                        TextColor="#333"
                        Padding="0"
                        Clicked="OnZoomInClicked"/>
            </Border>

            <Border StrokeShape="RoundRectangle 24"
                    Stroke="Transparent"
                    BackgroundColor="White"
                    WidthRequest="48"
                    HeightRequest="48">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/>
                </Border.Shadow>
                <Button Text="−"
                        FontSize="22"
                        FontAttributes="Bold"
                        BackgroundColor="Transparent"
                        TextColor="#333"
                        Padding="0"
                        Clicked="OnZoomOutClicked"/>
            </Border>

            <Border StrokeShape="RoundRectangle 24"
                    Stroke="Transparent"
                    BackgroundColor="White"
                    WidthRequest="48"
                    HeightRequest="48">
                <Border.Shadow>
                    <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/>
                </Border.Shadow>
                <Button Text="📍"
                        FontSize="20"
                        BackgroundColor="Transparent"
                        Padding="0"
                        Clicked="OnMyLocationClicked"/>
            </Border>
        </StackLayout>

        <!-- POI Detail Card -->
        <Border x:Name="PoiCard"
                IsVisible="False"
                VerticalOptions="End"
                HorizontalOptions="Fill"
                Margin="10,0,10,100"
                StrokeShape="RoundRectangle 16"
                Stroke="Transparent"
                BackgroundColor="White">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,4" Radius="12" Opacity="0.2"/>
            </Border.Shadow>
            <Grid RowDefinitions="180,Auto" ColumnDefinitions="*,Auto">

                <!-- Ảnh POI -->
                <Image x:Name="PoiImage"
                       Grid.Row="0" Grid.ColumnSpan="2"
                       Aspect="AspectFill"
                       HeightRequest="180"/>

                <!-- Thông tin -->
                <StackLayout Grid.Row="1" Grid.Column="0"
                             Padding="12,10"
                             Spacing="4">
                    <Label x:Name="PoiNameLabel"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#1A1A1A"/>
                    <Label x:Name="PoiDescLabel"
                           FontSize="13"
                           TextColor="#555"
                           MaxLines="2"
                           LineBreakMode="TailTruncation"/>
                    <Label x:Name="PoiCategoryLabel"
                           FontSize="11"
                           TextColor="#FF6B35"
                           FontAttributes="Bold"/>
                    <!-- Nút nghe TTS -->
                    <Button Text="🔊 Nghe thuyết minh"
                            FontSize="13"
                            BackgroundColor="#FF6B35"
                            TextColor="White"
                            CornerRadius="8"
                            Padding="12,6"
                            Margin="0,4,0,0"
                            Clicked="OnListenClicked"/>
                </StackLayout>

                <!-- Nút đóng -->
                <Button Grid.Row="1" Grid.Column="1"
                        Text="✕"
                        FontSize="16"
                        BackgroundColor="Transparent"
                        TextColor="#999"
                        WidthRequest="44"
                        HeightRequest="44"
                        Padding="0"
                        Clicked="OnClosePoiCard"/>
            </Grid>
        </Border>

        <!-- Free Trial Banner (Requirement 4.5) -->
        <Border x:Name="FreeTrialBanner"
                IsVisible="False"
                VerticalOptions="End"
                HorizontalOptions="Fill"
                Margin="10,0,10,56"
                StrokeShape="RoundRectangle 10"
                Stroke="Transparent"
                BackgroundColor="#FFF3E0">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.1"/>
            </Border.Shadow>
            <Label x:Name="FreeTrialLabel"
                   FontSize="13"
                   TextColor="#E65100"
                   HorizontalTextAlignment="Center"
                   Padding="12,8"/>
        </Border>

        <!-- Premium POI Bottom Sheet (Requirement 14.4) -->
        <Border x:Name="PremiumSheet"
                IsVisible="False"
                VerticalOptions="End"
                HorizontalOptions="Fill"
                StrokeShape="RoundRectangle 20,20,0,0"
                Stroke="Transparent"
                BackgroundColor="White">
            <Border.Shadow>
                <Shadow Brush="Black" Offset="0,-4" Radius="16" Opacity="0.25"/>
            </Border.Shadow>
            <StackLayout Padding="20,16,20,32" Spacing="12">

                <!-- Handle bar -->
                <BoxView WidthRequest="40"
                         HeightRequest="4"
                         CornerRadius="2"
                         Color="#DDD"
                         HorizontalOptions="Center"/>

                <!-- Lock icon + title -->
                <StackLayout Orientation="Horizontal" Spacing="10" HorizontalOptions="Center">
                    <Label Text="🔒" FontSize="28" VerticalOptions="Center"/>
                    <Label x:Name="PremiumPoiNameLabel"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#1A1A1A"
                           VerticalOptions="Center"/>
                </StackLayout>

                <Label x:Name="PremiumPoiDescLabel"
                       FontSize="13"
                       TextColor="#555"
                       MaxLines="3"
                       LineBreakMode="TailTruncation"
                       HorizontalTextAlignment="Center"/>

                <!-- Free trial info -->
                <Label x:Name="PremiumFreeTrialLabel"
                       IsVisible="False"
                       FontSize="13"
                       TextColor="#E65100"
                       HorizontalTextAlignment="Center"
                       FontAttributes="Italic"/>

                <Label Text="Nội dung này yêu cầu Access Pass để nghe thuyết minh."
                       FontSize="13"
                       TextColor="#777"
                       HorizontalTextAlignment="Center"/>

                <!-- Mua Access Pass button -->
                <Button x:Name="BuyAccessPassButton"
                        Text="🔓 Mua Access Pass ($1/7 ngày)"
                        FontSize="15"
                        FontAttributes="Bold"
                        BackgroundColor="#FF6B35"
                        TextColor="White"
                        CornerRadius="12"
                        HeightRequest="50"
                        Clicked="OnBuyAccessPassClicked"/>

                <!-- Đóng -->
                <Button Text="Đóng"
                        FontSize="14"
                        BackgroundColor="Transparent"
                        TextColor="#999"
                        Clicked="OnClosePremiumSheet"/>
            </StackLayout>
        </Border>
=======
    <Grid RowDefinitions="*,48">

        <!-- ===== TAB CONTENT ===== -->
        <Grid Grid.Row="0">

            <!-- ===== TAB BẢN ĐỒ ===== -->
            <Grid IsVisible="{Binding IsListTabActive, Converter={StaticResource InverseBoolConverter}}">

                <RefreshView IsRefreshing="{Binding IsRefreshing}"
                             Command="{Binding RefreshCommand}"
                             RefreshColor="#FF6B35">
                <maps:Map x:Name="MainMap"
                          MapType="Street"
                          IsShowingUser="True"
                          IsZoomEnabled="True"
                          IsScrollEnabled="True"/>
                </RefreshView>

                <!-- Overlay góc trên: Category + Language -->
                <Grid ColumnDefinitions="*,Auto"
                      VerticalOptions="Start"
                      HorizontalOptions="Fill"
                      Margin="10,48,10,0">
                    <Border Grid.Column="0" StrokeShape="RoundRectangle 20" Stroke="Transparent"
                            BackgroundColor="White" HeightRequest="40">
                        <Border.Shadow><Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/></Border.Shadow>
                        <Picker ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}"
                                Title="🍜 Loại quán" FontSize="13" Margin="12,0" VerticalOptions="Center"/>
                    </Border>
                    <Border Grid.Column="1" StrokeShape="RoundRectangle 20" Stroke="Transparent"
                            BackgroundColor="White" HeightRequest="40" WidthRequest="90" Margin="6,0,0,0">
                        <Border.Shadow><Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.15"/></Border.Shadow>
                        <Picker ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}"
                                Title="🌐" FontSize="12" Margin="8,0" VerticalOptions="Center"/>
                    </Border>
                </Grid>

                <!-- Zoom / MyLocation - ĐÃ XÓA theo yêu cầu -->


                <!-- POI Detail Card (bottom) -->
                <Border x:Name="PoiCard" IsVisible="False"
                        VerticalOptions="End" HorizontalOptions="Fill"
                        Margin="10,0,10,8"
                        StrokeShape="RoundRectangle 16" Stroke="Transparent" BackgroundColor="White">
                    <Border.Shadow><Shadow Brush="Black" Offset="0,4" Radius="12" Opacity="0.2"/></Border.Shadow>
                    <Grid RowDefinitions="160,Auto" ColumnDefinitions="*,Auto">
                        <Image x:Name="PoiImage" Grid.Row="0" Grid.ColumnSpan="2"
                               Aspect="AspectFill" HeightRequest="160"/>
                        <StackLayout Grid.Row="1" Grid.Column="0" Padding="12,10" Spacing="4">
                            <Label x:Name="PoiNameLabel" FontSize="16" FontAttributes="Bold" TextColor="#1A1A1A"/>
                            <Label x:Name="PoiDescLabel" FontSize="13" TextColor="#555"
                                   MaxLines="2" LineBreakMode="TailTruncation"/>
                            <Label x:Name="PoiCategoryLabel" FontSize="11" TextColor="#FF6B35" FontAttributes="Bold"/>
                            <Button Text="🔊 Nghe thuyết minh" FontSize="13"
                                    BackgroundColor="#FF6B35" TextColor="White"
                                    CornerRadius="8" Padding="12,6" Margin="0,4,0,0"
                                    Clicked="OnListenClicked"/>
                        </StackLayout>
                        <Button Grid.Row="1" Grid.Column="1" Text="✕" FontSize="16"
                                BackgroundColor="Transparent" TextColor="#999"
                                WidthRequest="44" HeightRequest="44" Padding="0"
                                Clicked="OnClosePoiCard"/>
                    </Grid>
                </Border>

                <!-- Premium Sheet -->
                <Border x:Name="PremiumSheet" IsVisible="False"
                        VerticalOptions="End" HorizontalOptions="Fill"
                        StrokeShape="RoundRectangle 20,20,0,0" Stroke="Transparent" BackgroundColor="White">
                    <Border.Shadow><Shadow Brush="Black" Offset="0,-4" Radius="16" Opacity="0.25"/></Border.Shadow>
                    <StackLayout Padding="20,16,20,32" Spacing="12">
                        <BoxView WidthRequest="40" HeightRequest="4" CornerRadius="2"
                                 Color="#DDD" HorizontalOptions="Center"/>
                        <StackLayout Orientation="Horizontal" Spacing="10" HorizontalOptions="Center">
                            <Label Text="🔒" FontSize="28" VerticalOptions="Center"/>
                            <Label x:Name="PremiumPoiNameLabel" FontSize="18" FontAttributes="Bold"
                                   TextColor="#1A1A1A" VerticalOptions="Center"/>
                        </StackLayout>
                        <Label x:Name="PremiumPoiDescLabel" FontSize="13" TextColor="#555"
                               MaxLines="3" LineBreakMode="TailTruncation" HorizontalTextAlignment="Center"/>
                        <Label x:Name="PremiumFreeTrialLabel" IsVisible="False" FontSize="13"
                               TextColor="#E65100" HorizontalTextAlignment="Center" FontAttributes="Italic"/>
                        <Label Text="Nội dung này yêu cầu Access Pass để nghe thuyết minh."
                               FontSize="13" TextColor="#777" HorizontalTextAlignment="Center"/>
                        <Button Text="🔓 Mua Access Pass ($1/7 ngày)" FontSize="15" FontAttributes="Bold"
                                BackgroundColor="#FF6B35" TextColor="White" CornerRadius="12"
                                HeightRequest="50" Clicked="OnBuyAccessPassClicked"/>
                        <Button Text="Đóng" FontSize="14" BackgroundColor="Transparent"
                                TextColor="#999" Clicked="OnClosePremiumSheet"/>
                    </StackLayout>
                </Border>

            </Grid>

            <!-- ===== TAB DANH SÁCH ===== -->
            <Grid RowDefinitions="Auto,Auto,Auto,*"
                  IsVisible="{Binding IsListTabActive}"
                  BackgroundColor="#F5F5F5">

                <!-- Free Trial Banner - trên cùng -->
                <Border x:Name="FreeTrialBanner" Grid.Row="0" IsVisible="False"
                        StrokeShape="RoundRectangle 0" Stroke="Transparent" BackgroundColor="#FF6B35"
                        Margin="0,0,0,0">
                    <Label x:Name="FreeTrialLabel" FontSize="13" TextColor="White"
                           HorizontalTextAlignment="Center" Padding="12,10"
                           FontAttributes="Bold"/>
                </Border>

                <!-- Search bar -->
                <Border Grid.Row="1"
                        StrokeShape="RoundRectangle 12" Stroke="Transparent"
                        BackgroundColor="White" Margin="12,52,12,0">
                    <Border.Shadow><Shadow Brush="Black" Offset="0,2" Radius="4" Opacity="0.1"/></Border.Shadow>
                    <SearchBar Placeholder="🔍 Tìm quán ăn..."
                               Text="{Binding SearchText}"
                               FontSize="14" BackgroundColor="Transparent"
                               CancelButtonColor="#FF6B35"/>
                </Border>

                <!-- Category chips -->
                <ScrollView Grid.Row="2" Orientation="Horizontal"
                            HorizontalScrollBarVisibility="Never" Margin="8,8,8,0">
                    <HorizontalStackLayout x:Name="CategoryChips" Spacing="8" Padding="4,0"/>
                </ScrollView>

                <!-- POI List -->
                <RefreshView Grid.Row="3"
                             IsRefreshing="{Binding IsRefreshing}"
                             Command="{Binding RefreshCommand}"
                             RefreshColor="#FF6B35">
                <CollectionView ItemsSource="{Binding FilteredPois}" Margin="0,8,0,0">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border StrokeShape="RoundRectangle 12" Stroke="Transparent"
                                    BackgroundColor="White" Margin="12,0,12,10">
                                <Border.Shadow>
                                    <Shadow Brush="Black" Offset="0,2" Radius="6" Opacity="0.08"/>
                                </Border.Shadow>
                                <Grid ColumnDefinitions="80,*" RowDefinitions="Auto,Auto,Auto"
                                      Padding="0,0,0,0">

                                    <!-- Ảnh -->
                                    <Image Grid.RowSpan="3" Source="{Binding ImagePath}"
                                           Aspect="AspectFill" WidthRequest="80" HeightRequest="90"/>

                                    <!-- Tên -->
                                    <Label Grid.Column="1" Grid.Row="0"
                                           Text="{Binding Name}"
                                           FontSize="15" FontAttributes="Bold" TextColor="#1A1A1A"
                                           Margin="10,10,8,2" MaxLines="1" LineBreakMode="TailTruncation"/>

                                    <!-- Mô tả -->
                                    <Label Grid.Column="1" Grid.Row="1"
                                           Text="{Binding Description}"
                                           FontSize="12" TextColor="#666"
                                           Margin="10,0,8,4" MaxLines="2" LineBreakMode="TailTruncation"/>

                                    <!-- Category badge + 2 nút -->
                                    <Grid Grid.Column="1" Grid.Row="2"
                                          ColumnDefinitions="*,Auto,Auto"
                                          Margin="10,0,8,10">

                                        <!-- Category badge với màu -->
                                        <Border Grid.Column="0"
                                                x:Name="CategoryBadge"
                                                StrokeShape="RoundRectangle 10"
                                                Stroke="Transparent"
                                                Padding="6,3"
                                                HorizontalOptions="Start"
                                                VerticalOptions="Center"
                                                BackgroundColor="{Binding CategoryBadgeColor}">
                                            <Label Text="{Binding CategoryDisplayName}"
                                                   FontSize="10" FontAttributes="Bold"
                                                   TextColor="{Binding CategoryTextColor}"/>
                                        </Border>

                                        <!-- Nút nghe -->
                                        <Button Grid.Column="1"
                                                Text="🔊"
                                                FontSize="16"
                                                BackgroundColor="#FFF3E0"
                                                TextColor="#FF6B35"
                                                CornerRadius="8"
                                                WidthRequest="36"
                                                HeightRequest="30"
                                                Padding="0"
                                                Margin="4,0,0,0"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ListenCommand}"
                                                CommandParameter="{Binding .}"/>

                                        <!-- Nút bản đồ -->
                                        <Button Grid.Column="2"
                                                Text="📍"
                                                FontSize="16"
                                                BackgroundColor="#FF6B35"
                                                TextColor="White"
                                                CornerRadius="8"
                                                WidthRequest="36"
                                                HeightRequest="30"
                                                Padding="0"
                                                Margin="4,0,0,0"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShowOnMapCommand}"
                                                CommandParameter="{Binding .}"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center"
                                     Spacing="8" Margin="0,60,0,0">
                            <Label Text="🍜" FontSize="48" HorizontalOptions="Center"/>
                            <Label Text="Không tìm thấy quán nào" FontSize="15" TextColor="#999"
                                   HorizontalOptions="Center"/>
                        </StackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
                </RefreshView>

            </Grid>

        </Grid>

        <!-- ===== TAB BAR ===== -->
        <Grid Grid.Row="1" ColumnDefinitions="*,*" BackgroundColor="White">
            <Grid.Shadow><Shadow Brush="Black" Offset="0,-2" Radius="8" Opacity="0.1"/></Grid.Shadow>
            <Button Grid.Column="0" x:Name="MapTabBtn" Text="🗺️ Bản đồ"
                    FontSize="13" BackgroundColor="Transparent" TextColor="#FF6B35"
                    FontAttributes="Bold" Clicked="OnMapTabClicked"/>
            <Button Grid.Column="1" x:Name="ListTabBtn" Text="📋 Danh sách"
                    FontSize="13" BackgroundColor="Transparent" TextColor="#999"
                    Clicked="OnListTabClicked"/>
        </Grid>
>>>>>>> bb1d8ae5 (feat: UI improvements, device trial, category fix, pull-to-refresh, map pin card)

    </Grid>

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:InverseBoolConverter x:Key="InverseBoolConverter"
                                        xmlns:local="clr-namespace:VinhKhanh.Mobile.Views"/>
        </ResourceDictionary>
    </ContentPage.Resources>

</ContentPage>
